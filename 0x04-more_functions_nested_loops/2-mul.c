#include "main.h"
/**
 * mul - multiplies two integers
 *@a: first integer to be mutliplied
 *@b: second integer to be multiplied
 *
 * Return: the value of the multiplied integers
 */
int mul(int a, int b)
{
	return (a * b);
}
