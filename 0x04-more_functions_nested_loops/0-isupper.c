#include "main.h"
/**
 * _isupper - determines if character is uppercase
 *@c: the character to be determined
 *
 * Return: 1 if c is uppercase, else 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
}
}
