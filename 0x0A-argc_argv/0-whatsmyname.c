#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 *@argc: number of arguments
 *@argv: array of arguments
 *
 *Return: 0 success, always
 */
int main(int argc, char *argv[])
{
	if (argc >= 1)
	{
	printf("%s\n", argv[0]);
	}
	return (0);
}
