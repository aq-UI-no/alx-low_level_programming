#include "main.h"
#include <stdio.h>
/**
* main - entry point
* @argc: number of arguments
* @argv: array of arguments
*
* Return: 0 always, success
*/
int main(int argc, char *argv[])
{
(void)argv;
{
if (argc >= 1)
{
printf("%d\n", argc - 1);
}
}
return (0);
}
